name: tunnelmate
description: 掘进一线技术员辅助管理App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.1.0
  intl: ^0.18.0
  path: ^1.8.3
  share_plus: ^7.0.0
  url_launcher: ^6.1.0
  file_picker: ^6.0.0
  csv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
